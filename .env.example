# Docker Compose interpolation for the coturn service.
# The server exposes this to browsers at /api/client-config at runtime.
TURN_USER=replace-with-turn-username:replace-with-turn-password
TURN_HOST=turn.example.com
